Added `xkb_server_options::xkb_server_options_remap_mods()` to enable remapping modifiers combos, e.g. to make `Control+Alt` map to `LevelThree` (`AltGr`).
This help improving *compatibility* across platforms.
