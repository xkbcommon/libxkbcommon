Added `xkb_state_machine_options_mods_set_mapping()` to enable remapping
modifiers combos, e.g. to make `Control+Alt` map to `LevelThree` (`AltGr`).
This help improving *compatibility* across platforms.
