Added `xkb_has_feature()` and `enum xkb_feature` to test feature availability,
which is useful when the library is dynamically linked.
Currently it supports only testing enumerations and their values.

